services:
  postgres:
    image: postgres
    restart: always
    volumes:
      - ./postgres-data:/var/lib/postgresql/data
    ports:
      - '5432:5432'
    environment:
      POSTGRES_USER: nest_shop
      POSTGRES_PASSWORD: nest_shop
      POSTGRES_DB: nest_shop